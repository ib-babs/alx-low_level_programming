#include "main.h"
#include <string.h>
/**
 * _strlen - Return the length of the input string
 * @s: Pointer to the string
 * Return: Length of the string
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
