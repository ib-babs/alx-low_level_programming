#include <string.h>
/**
 * _strchr - Main pointer entry
 * @s: String pointer to test
 * @c: Character to process
 * Return: String location
*/
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
