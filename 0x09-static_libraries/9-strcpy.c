#include <string.h>
#include "main.h"
/**
 * _strcpy - copy string from one holder to another
 * @dest: destination
 * @src: source
 * Return: src copied to  destination
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
