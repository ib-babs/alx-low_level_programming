#include <string.h>
/**
 * _strcat - main entry point
 * @dest: Destination
 * @src: Sources
 * Return: Concatenate of string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
